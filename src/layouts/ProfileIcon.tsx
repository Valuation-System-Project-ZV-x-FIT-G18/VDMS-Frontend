/* Profile icon button — circular avatar placeholder in the top bar */
const ProfileIcon = () => (
  <button className="topbar-profile" title="Profile">
    👤                              {/* user emoji as profile avatar */}
  </button>
);

export default ProfileIcon;
