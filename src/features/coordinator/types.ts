/* Defines the shape of each role shown on the landing page */
export interface Role {
  id: string;       // unique key used for routing later
  label: string;    // display text on the button
  color: string;    // background colour for the button
}
